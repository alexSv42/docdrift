{
  "name": "docdrift",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "docdrift": "./src/cli.ts"
  },
  "scripts": {
    "docdrift": "tsx --env-file-if-exists=.env src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@ai-sdk/gateway": "^2",
    "@redocly/cli": "^2",
    "ai": "^5",
    "zod": "^3"
  },
  "devDependencies": {
    "@types/node": "^24",
    "tsx": "^4",
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
